# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=mnemosyne
DB_PASSWORD=mnemosyne
DB_NAME=mnemosyne

# Redis
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# Server
PORT=8080

# Frontend (if running separately)
PUBLIC_API_URL=http://localhost:8080/api/v1

# GitHub Integration (for future use)
GITHUB_TOKEN=
GITHUB_REPO=
GITHUB_BRANCH=main